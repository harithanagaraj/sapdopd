
#include <stdio.h>
#include<conio.h>
int main()
{
    long long n;
    int count = 0;

    printf("Enter an integer: ");
    scanf("%lld", &n);

    while(n != 0)
    {
    int(i=0;i++;i=10);

    
        
       
    }

    printf("Number of digits: %d", count);
}
